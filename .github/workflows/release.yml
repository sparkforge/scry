name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact_name: forge
            asset_name: forge-linux-x86_64

          - target: aarch64-apple-darwin
            os: macos-latest
            artifact_name: forge
            asset_name: forge-macos-arm64

          - target: x86_64-apple-darwin
            os: macos-latest
            artifact_name: forge
            asset_name: forge-macos-x86_64

          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact_name: forge.exe
            asset_name: forge-windows-x86_64.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-action@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          # Linux
          cp artifacts/forge-linux-x86_64/forge release/forge-linux-x86_64
          chmod +x release/forge-linux-x86_64
          # macOS ARM64
          cp artifacts/forge-macos-arm64/forge release/forge-macos-arm64
          chmod +x release/forge-macos-arm64
          # macOS x86_64
          cp artifacts/forge-macos-x86_64/forge release/forge-macos-x86_64
          chmod +x release/forge-macos-x86_64
          # Windows
          cp artifacts/forge-windows-x86_64.exe/forge.exe release/forge-windows-x86_64.exe

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release/forge-linux-x86_64
            release/forge-macos-arm64
            release/forge-macos-x86_64
            release/forge-windows-x86_64.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
